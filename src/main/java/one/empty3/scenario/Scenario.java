package one.empty3.scenario;

public class Scenario {
    // getScenarios(Representable r);
    // Point3D move rotateAround
    // Line P3 P3 move rotateAround
         // MorphTo Polynomial PolySegment Extract
        //  AddTo PolynomialCurve as Line in Polynomial Surface
    // Point List, add at, remove at, edit all, edit one, view browser, edit cordinates
    // Views 2D 3D standard, curve/surface tangent view, upView, ...
    // Surface Cut cordinates, cut from 2D shape, drawOnly 2D shape, colors drawing or bitmap
    // Curve Cut cordinates, drawOnly from/to&
    // Start a new Object from 2D 1D / 2D 1D shapes at *
    // Thickness, border of surface or curve
    // extrude, build on -> matrix adapt.

}
